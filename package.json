{
  "name": "JATE",
  "version": "1.0.0",
  "description": "Just another text editor using PWA tools.",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"cd client && npm run build\" \"cd server && npm run server\" ",
    "server": "npm run build && cd server && node server.js",
    "build": "cd server node server.js --ignore client",
    "install": "cd client && npm run build",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
